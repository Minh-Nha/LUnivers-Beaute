<UserControl x:Class="LUnivers_Beaute.Views.HoaDonView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">
    <Grid Margin="24">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2.5*"/>
            <ColumnDefinition Width="1*" MinWidth="400"/>
        </Grid.ColumnDefinitions>

        <!-- LEFT PANEL: Invoices List -->
        <Grid Grid.Column="0" Margin="0,0,16,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <Grid Grid.Row="0" Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="Quản lý Hóa đơn" Style="{StaticResource SectionTitle}"/>
                    <TextBlock Text="Lịch sử giao dịch và hóa đơn bán hàng" FontSize="13" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,4,0,0"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Button Content="📥 Xuất báo cáo" Style="{StaticResource SecondaryButton}" Padding="14,10"/>
                </StackPanel>
            </Grid>
            
            <Border Grid.Row="1" Style="{StaticResource CardBorder}" Padding="16" Margin="0,0,0,16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox Style="{StaticResource ModernTextBox}" Margin="0,0,10,0"/>
                    <DatePicker Grid.Column="1" Margin="0,0,10,0" FontSize="13" Style="{StaticResource ModernDatePicker}"/>
                    <DatePicker Grid.Column="2" Margin="0,0,10,0" FontSize="13" Style="{StaticResource ModernDatePicker}"/>
                    <Button Grid.Column="3" Content="🔍 Tìm kiếm" Style="{StaticResource PrimaryButton}" Padding="16,10"/>
                </Grid>
            </Border>
            
            <Border Grid.Row="2" Style="{StaticResource CardBorder}" Padding="0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="🧾 Danh sách hóa đơn" FontSize="15" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" Margin="20,20,0,16"/>
                    <DataGrid Grid.Row="1" AutoGenerateColumns="False" x:Name="dgData" Style="{StaticResource ModernDataGrid}" 
                              ColumnHeaderStyle="{StaticResource ModernDataGridColumnHeader}"
                              RowStyle="{StaticResource ModernDataGridRow}" 
                              CellStyle="{StaticResource ModernDataGridCell}"
                              SelectionChanged="DgData_SelectionChanged">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="MÃ HĐ" Binding="{Binding MaHoaDon}" Width="90"/>
                            <DataGridTextColumn Header="KHÁCH HÀNG" Binding="{Binding KhachHang}" Width="130"/>
                            <DataGridTextColumn Header="NGÀY LẬP" Binding="{Binding NgayLap}" Width="140"/>
                            <DataGridTextColumn Header="TỔNG TIỀN" Binding="{Binding TongTien, StringFormat={}{0:#,##0} đ}" Width="*"/>
                        </DataGrid.Columns>
                    </DataGrid>
                    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,12,0,12">
                        <Button x:Name="btnPrevPage" Content="&lt;" Style="{StaticResource SecondaryButton}" Width="36" Height="36" Padding="0" Click="BtnPrevPage_Click"/>
                        <TextBlock x:Name="txtPageInfo" Text="Trang 1 / 1" VerticalAlignment="Center" Margin="16,0" FontWeight="SemiBold"/>
                        <Button x:Name="btnNextPage" Content="&gt;" Style="{StaticResource SecondaryButton}" Width="36" Height="36" Padding="0" Click="BtnNextPage_Click"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>

        <!-- RIGHT PANEL: Invoice Details -->
        <Border Grid.Column="1" Style="{StaticResource CardBorder}" Padding="20" x:Name="detailPanel" Visibility="Collapsed">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <!-- Header -->
                <Grid Grid.Row="0" Margin="0,0,0,20">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="✦ L'UNIVERS BEAUTÉ ✦" FontSize="14" FontWeight="Bold" Foreground="{StaticResource AccentPeachBrush}" HorizontalAlignment="Center" Margin="0,0,0,4"/>
                        <TextBlock Text="CHI TIẾT HÓA ĐƠN" FontSize="18" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Grid>

                <!-- Info -->
                <Border Grid.Row="1" Background="#F8F9FA" CornerRadius="8" Padding="14" Margin="0,0,0,16">
                    <StackPanel>
                        <Grid Margin="0,0,0,6">
                            <TextBlock Text="Mã HĐ:" Foreground="{StaticResource TextSecondaryBrush}"/>
                            <TextBlock x:Name="lblMaHD" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" HorizontalAlignment="Right"/>
                        </Grid>
                        <Grid Margin="0,0,0,6">
                            <TextBlock Text="Ngày lập:" Foreground="{StaticResource TextSecondaryBrush}"/>
                            <TextBlock x:Name="lblNgayLap" Foreground="{StaticResource TextPrimaryBrush}" HorizontalAlignment="Right"/>
                        </Grid>
                        <Grid Margin="0,0,0,6">
                            <TextBlock Text="Thu ngân:" Foreground="{StaticResource TextSecondaryBrush}"/>
                            <TextBlock x:Name="lblNhanVien" Foreground="{StaticResource TextPrimaryBrush}" HorizontalAlignment="Right"/>
                        </Grid>
                        <Grid Margin="0,0,0,6">
                            <TextBlock Text="Khách hàng:" Foreground="{StaticResource TextSecondaryBrush}"/>
                            <TextBlock x:Name="lblKhachHang" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" HorizontalAlignment="Right"/>
                        </Grid>
                        <Grid Margin="0,0,0,6">
                            <TextBlock Text="Thanh toán:" Foreground="{StaticResource TextSecondaryBrush}"/>
                            <TextBlock x:Name="lblThanhToan" Foreground="{StaticResource TextPrimaryBrush}" HorizontalAlignment="Right"/>
                        </Grid>
                        <Grid>
                            <TextBlock Text="Cửa hàng:" Foreground="{StaticResource TextSecondaryBrush}"/>
                            <TextBlock x:Name="lblCuaHang" Foreground="{StaticResource TextPrimaryBrush}" HorizontalAlignment="Right"/>
                        </Grid>
                    </StackPanel>
                </Border>
                
                <!-- Items Grid -->
                <Border Grid.Row="2" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="0" Margin="0,0,0,16">
                    <DataGrid AutoGenerateColumns="False" x:Name="dgChiTiet" Style="{StaticResource ModernDataGrid}"
                              ColumnHeaderStyle="{StaticResource ModernDataGridColumnHeader}"
                              RowStyle="{StaticResource ModernDataGridRow}" CellStyle="{StaticResource ModernDataGridCell}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="SẢN PHẨM" Binding="{Binding TenSanPham}" Width="*">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="TextWrapping" Value="Wrap"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="SL" Binding="{Binding SoLuong}" Width="40"/>
                            <DataGridTextColumn Header="THÀNH TIỀN" Binding="{Binding ThanhTien, StringFormat={}{0:#,##0}}" Width="90"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>

                <!-- Summary -->
                <StackPanel Grid.Row="3" Margin="0,0,0,20">
                    <Grid Margin="0,0,0,8">
                        <TextBlock Text="Tổng tiền:" Foreground="{StaticResource TextSecondaryBrush}"/>
                        <TextBlock x:Name="lblTongTien" Text="0 đ" HorizontalAlignment="Right" FontWeight="SemiBold"/>
                    </Grid>
                    <Grid Margin="0,0,0,12">
                        <TextBlock Text="Khuyến mãi:" Foreground="{StaticResource TextSecondaryBrush}"/>
                        <TextBlock x:Name="lblKhuyenMai" Text="0 đ" HorizontalAlignment="Right" Foreground="{StaticResource AccentPeachBrush}"/>
                    </Grid>
                    <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0" Margin="0,0,0,12"/>
                    <Grid>
                        <TextBlock Text="KHÁCH TRẢ:" FontWeight="Bold" FontSize="14" Foreground="{StaticResource TextPrimaryBrush}"/>
                        <TextBlock x:Name="lblCanTra" Text="0 đ" HorizontalAlignment="Right" FontWeight="Bold" FontSize="16" Foreground="{StaticResource AccentPeachBrush}"/>
                    </Grid>
                </StackPanel>

                <!-- Actions -->
                <Button Grid.Row="4" Content="🖨️ IN HÓA ĐƠN" Style="{StaticResource PrimaryButton}" Height="40" FontSize="13" Click="BtnPrint_Click"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>


