<UserControl x:Class="LUnivers_Beaute.Views.HoaDonView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">
    <Grid>
        <Grid Margin="28,24" x:Name="mainContent">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="Quản lý Hóa đơn" Style="{StaticResource SectionTitle}"/>
                    <TextBlock Text="Lịch sử giao dịch và hóa đơn bán hàng" FontSize="13" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,4,0,0"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Button Content="📥 Xuất báo cáo" Style="{StaticResource SecondaryButton}" Padding="14,10"/>
                </StackPanel>
            </Grid>
            
            <Border Grid.Row="1" Style="{StaticResource CardBorder}" Padding="16" Margin="0,0,0,16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox Style="{StaticResource ModernTextBox}" Margin="0,0,10,0"/>
                    <DatePicker Grid.Column="1" Margin="0,0,10,0" FontSize="13" Style="{StaticResource ModernDatePicker}"/>
                    <DatePicker Grid.Column="2" Margin="0,0,10,0" FontSize="13" Style="{StaticResource ModernDatePicker}"/>
                    <Button Grid.Column="3" Content="🔍 Tìm kiếm" Style="{StaticResource PrimaryButton}" Padding="16,10"/>
                </Grid>
            </Border>
            
            <Border Grid.Row="2" Style="{StaticResource CardBorder}" Padding="0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="🧾 Danh sách hóa đơn" FontSize="15" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" Margin="20,20,0,16"/>
                    <Grid><Grid.RowDefinitions><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions><DataGrid AutoGenerateColumns="False" x:Name="dgData" Grid.Row="0" Style="{StaticResource ModernDataGrid}" 
                              ColumnHeaderStyle="{StaticResource ModernDataGridColumnHeader}"
                              RowStyle="{StaticResource ModernDataGridRow}" 
                              CellStyle="{StaticResource ModernDataGridCell}"
                              SelectionChanged="DgData_SelectionChanged">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="MÃ HĐ" Binding="{Binding MaHoaDon}" Width="100"/>
                            <DataGridTextColumn Header="CỬA HÀNG" Binding="{Binding TenCuaHang}" Width="130"/>
                            <DataGridTextColumn Header="NHÂN VIÊN" Binding="{Binding NhanVienLap}" Width="140"/>
                            <DataGridTextColumn Header="KHÁCH HÀNG" Binding="{Binding KhachHang}" Width="140"/>
                            <DataGridTextColumn Header="NGÀY LẬP" Binding="{Binding NgayLap}" Width="140"/>
                            <DataGridTextColumn Header="TỔNG TIỀN" Binding="{Binding TongTien, StringFormat={}{0:#,##0} đ}" Width="120"/>
                            <DataGridTextColumn Header="KHÁCH TRẢ" Binding="{Binding KhachCanTra, StringFormat={}{0:#,##0} đ}" Width="120"/>
                            <DataGridTemplateColumn Header="THAO TÁC" Width="100">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="👁️ Chi tiết" Style="{StaticResource SecondaryButton}" Padding="8,4" FontSize="11" Click="BtnViewDetail_Click"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,12,0,12">
            <Button x:Name="btnPrevPage" Content="&lt;" Style="{StaticResource SecondaryButton}" Width="36" Height="36" Padding="0" Click="BtnPrevPage_Click"/>
            <TextBlock x:Name="txtPageInfo" Text="Trang 1 / 1" VerticalAlignment="Center" Margin="16,0" FontWeight="SemiBold"/>
            <Button x:Name="btnNextPage" Content="&gt;" Style="{StaticResource SecondaryButton}" Width="36" Height="36" Padding="0" Click="BtnNextPage_Click"/>
        </StackPanel>
</Grid>
                </Grid>
            </Border>
        </Grid>

        <!-- Overlay Invoice Receipt Modal -->
        <Grid x:Name="detailPanel" Visibility="Collapsed">
            <Border Background="#66000000" MouseDown="Overlay_MouseDown"/>
            
            <Border HorizontalAlignment="Center" VerticalAlignment="Center" Width="600" MaxHeight="750" 
                    Style="{StaticResource CardBorder}" Padding="30" Background="White">
                <Border.Effect>
                    <DropShadowEffect Color="Black" Direction="270" ShadowDepth="10" BlurRadius="40" Opacity="0.15"/>
                </Border.Effect>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <!-- Header -->
                    <Grid Grid.Row="0" Margin="0,0,0,20">
                        <StackPanel HorizontalAlignment="Center">
                            <TextBlock Text="✦ L'UNIVERS BEAUTÉ ✦" FontSize="18" FontWeight="Bold" Foreground="{StaticResource AccentPeachBrush}" HorizontalAlignment="Center" Margin="0,0,0,4"/>
                            <TextBlock Text="HÓA ĐƠN BÁN HÀNG" FontSize="22" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" HorizontalAlignment="Center"/>
                        </StackPanel>
                        <Button Content="✕" HorizontalAlignment="Right" VerticalAlignment="Top" 
                                Background="Transparent" BorderThickness="0" Foreground="{StaticResource TextSecondaryBrush}" FontSize="18" Cursor="Hand" Click="BtnCloseModal_Click"/>
                    </Grid>

                    <!-- Info -->
                    <Border Grid.Row="1" Background="#F8F9FA" CornerRadius="8" Padding="16" Margin="0,0,0,20">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                    <TextBlock Text="Mã HD: " Foreground="{StaticResource TextSecondaryBrush}" Width="70"/>
                                    <TextBlock x:Name="lblMaHD" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                    <TextBlock Text="Ngày lập: " Foreground="{StaticResource TextSecondaryBrush}" Width="70"/>
                                    <TextBlock x:Name="lblNgayLap" Foreground="{StaticResource TextPrimaryBrush}"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="Thu ngân: " Foreground="{StaticResource TextSecondaryBrush}" Width="70"/>
                                    <TextBlock x:Name="lblNhanVien" Foreground="{StaticResource TextPrimaryBrush}"/>
                                </StackPanel>
                            </StackPanel>
                            
                            <StackPanel Grid.Column="1">
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                    <TextBlock Text="Khách hàng: " Foreground="{StaticResource TextSecondaryBrush}" Width="80"/>
                                    <TextBlock x:Name="lblKhachHang" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                                    <TextBlock Text="Thanh toán: " Foreground="{StaticResource TextSecondaryBrush}" Width="80"/>
                                    <TextBlock x:Name="lblThanhToan" Foreground="{StaticResource TextPrimaryBrush}"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="Cửa hàng: " Foreground="{StaticResource TextSecondaryBrush}" Width="80"/>
                                    <TextBlock x:Name="lblCuaHang" Foreground="{StaticResource TextPrimaryBrush}"/>
                                </StackPanel>
                            </StackPanel>
                        </Grid>
                    </Border>
                    
                    <!-- Items Grid -->
                    <Border Grid.Row="2" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="0" Margin="0,0,0,20">
                        <DataGrid AutoGenerateColumns="False" x:Name="dgChiTiet" Style="{StaticResource ModernDataGrid}"
                                  ColumnHeaderStyle="{StaticResource ModernDataGridColumnHeader}"
                                  RowStyle="{StaticResource ModernDataGridRow}" CellStyle="{StaticResource ModernDataGridCell}">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="SẢN PHẨM" Binding="{Binding TenSanPham}" Width="*"/>
                                <DataGridTextColumn Header="SL" Binding="{Binding SoLuong}" Width="50"/>
                                <DataGridTextColumn Header="ĐƠN GIÁ" Binding="{Binding DonGia, StringFormat={}{0:#,##0}}" Width="90"/>
                                <DataGridTextColumn Header="THÀNH TIỀN" Binding="{Binding ThanhTien, StringFormat={}{0:#,##0}}" Width="100"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>

                    <!-- Summary -->
                    <StackPanel Grid.Row="3" HorizontalAlignment="Right" Margin="0,0,0,20" MinWidth="200">
                        <Grid Margin="0,0,0,8">
                            <TextBlock Text="Tổng tiền:" Foreground="{StaticResource TextSecondaryBrush}"/>
                            <TextBlock x:Name="lblTongTien" Text="0 đ" HorizontalAlignment="Right" FontWeight="SemiBold"/>
                        </Grid>
                        <Grid Margin="0,0,0,12">
                            <TextBlock Text="Khuyến mãi:" Foreground="{StaticResource TextSecondaryBrush}"/>
                            <TextBlock x:Name="lblKhuyenMai" Text="0 đ" HorizontalAlignment="Right" Foreground="{StaticResource AccentPeachBrush}"/>
                        </Grid>
                        <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0" Margin="0,0,0,12"/>
                        <Grid>
                            <TextBlock Text="KHÁCH CẦN TRẢ:" FontWeight="Bold" FontSize="14" Foreground="{StaticResource TextPrimaryBrush}"/>
                            <TextBlock x:Name="lblCanTra" Text="0 đ" HorizontalAlignment="Right" FontWeight="Bold" FontSize="16" Foreground="{StaticResource AccentPeachBrush}"/>
                        </Grid>
                    </StackPanel>

                    <!-- Actions -->
                    <Button Grid.Row="4" Content="🖨️ IN HÓA ĐƠN" Style="{StaticResource PrimaryButton}" Height="46" FontSize="14"/>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>


