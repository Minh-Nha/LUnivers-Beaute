<UserControl x:Class="LUnivers_Beaute.Views.KhuyenMaiView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">
    <Grid>
        <Grid Margin="28,24" x:Name="mainContent">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="Chương trình Khuyến mãi" Style="{StaticResource SectionTitle}"/>
                    <TextBlock Text="Quản lý giảm giá và ưu đãi" FontSize="13" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,4,0,0"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Button x:Name="btnExportExcel" Content="📥 Xuất Excel" Style="{StaticResource SecondaryButton}" Margin="0,0,8,0" Padding="14,10" Click="BtnExportExcel_Click"/>
                    <Button x:Name="btnAdd" Content="＋ Thêm khuyến mãi" Style="{StaticResource PrimaryButton}" Padding="16,10" Click="BtnAdd_Click"/>
                </StackPanel>
            </Grid>
            <Border Grid.Row="1" Style="{StaticResource CardBorder}" Padding="16" Margin="0,0,0,16">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="10"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="1*"/>
                        <ColumnDefinition Width="1.5*"/>
                    </Grid.ColumnDefinitions>

                    <TextBox x:Name="txtSearch" Grid.Row="0" Grid.Column="0" Style="{StaticResource ModernTextBox}" Margin="0,0,10,0" Tag="Tìm kiếm tên chương trình..."/>
                    
                    <ComboBox x:Name="cboSearchDanhMuc" Grid.Row="0" Grid.Column="1" Style="{StaticResource ModernComboBox}" Margin="0,0,10,0" SelectedValuePath="MaDanhMuc">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding TenDanhMuc}"/>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>

                    <ComboBox x:Name="cboSearchLoaiGiam" Grid.Row="0" Grid.Column="2" Style="{StaticResource ModernComboBox}" Margin="0,0,10,0">
                        <ComboBoxItem Content="Tất cả loại giảm" IsSelected="True"/>
                        <ComboBoxItem Content="%"/>
                        <ComboBoxItem Content="VND"/>
                    </ComboBox>
                    
                    <ComboBox x:Name="cboSearchTrangThai" Grid.Row="0" Grid.Column="3" Style="{StaticResource ModernComboBox}">
                        <ComboBoxItem Content="Tất cả trạng thái" IsSelected="True"/>
                        <ComboBoxItem Content="Đang diễn ra"/>
                        <ComboBoxItem Content="Chưa bắt đầu"/>
                        <ComboBoxItem Content="Đã kết thúc"/>
                    </ComboBox>

                    <Grid Grid.Row="2" Grid.ColumnSpan="4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Orientation="Horizontal" Grid.Column="0" Margin="0,0,24,0">
                            <TextBlock Text="Từ ngày:" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="{StaticResource TextSecondaryBrush}"/>
                            <DatePicker x:Name="dpSearchTuNgay" Style="{StaticResource ModernDatePicker}" Width="150"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Grid.Column="1">
                            <TextBlock Text="Đến ngày:" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="{StaticResource TextSecondaryBrush}"/>
                            <DatePicker x:Name="dpSearchDenNgay" Style="{StaticResource ModernDatePicker}" Width="150"/>
                        </StackPanel>
                        <Button x:Name="btnSearch" Grid.Column="3" Content="🔍 Tìm kiếm" Style="{StaticResource PrimaryButton}" Padding="24,10" Click="BtnSearch_Click"/>
                    </Grid>
                </Grid>
            </Border>
            <Border Grid.Row="2" Style="{StaticResource CardBorder}" Padding="0">
                <Grid><Grid.RowDefinitions><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions><DataGrid AutoGenerateColumns="False" x:Name="dgData" Style="{StaticResource ModernDataGrid}"
                          ColumnHeaderStyle="{StaticResource ModernDataGridColumnHeader}"
                          RowStyle="{StaticResource ModernDataGridRow}"
                          CellStyle="{StaticResource ModernDataGridCell}"
                          SelectionChanged="DgData_SelectionChanged">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="MÃ" Binding="{Binding MaKhuyenMai}" Width="60"/>
                        <DataGridTextColumn Header="TÊN CHƯƠNG TRÌNH" Binding="{Binding TenChuongTrinh}" Width="*"/>
                        <DataGridTextColumn Header="GIÁ TRỊ GIẢM" Binding="{Binding MucGiam}" Width="110"/>
                        <DataGridTextColumn Header="LOẠI GIẢM" Binding="{Binding LoaiGiam}" Width="90"/>
                        <DataGridTextColumn Header="ÁP DỤNG" Binding="{Binding ApDungTheoHienThi}" Width="100"/>
                        <DataGridTextColumn Header="BẮT ĐẦU" Binding="{Binding NgayBatDau}" Width="110"/>
                        <DataGridTextColumn Header="KẾT THÚC" Binding="{Binding NgayKetThuc}" Width="110"/>
                        <DataGridTextColumn Header="TRẠNG THÁI" Binding="{Binding TinhTrang}" Width="100"/>
                        <DataGridTemplateColumn Header="THAO TÁC" Width="120">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Content="✏️" Style="{StaticResource IconButton}" Width="30" Height="30" Click="BtnEdit_Click"/>
                                        <Button Content="🗑️" Style="{StaticResource IconButton}" Width="30" Height="30" Click="BtnDelete_Click"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,12,0,12">
            <Button x:Name="btnPrevPage" Content="&lt;" Style="{StaticResource SecondaryButton}" Width="36" Height="36" Padding="0" Click="BtnPrevPage_Click"/>
            <TextBlock x:Name="txtPageInfo" Text="Trang 1 / 1" VerticalAlignment="Center" Margin="16,0" FontWeight="SemiBold"/>
            <Button x:Name="btnNextPage" Content="&gt;" Style="{StaticResource SecondaryButton}" Width="36" Height="36" Padding="0" Click="BtnNextPage_Click"/>
        </StackPanel>
</Grid>
            </Border>
        </Grid>

        <!-- Overlay Modal Dialog -->
        <Grid x:Name="crudPanel" Visibility="Collapsed">
            <Border Background="#66000000" MouseDown="Overlay_MouseDown"/>
            
            <Border HorizontalAlignment="Center" VerticalAlignment="Center" Width="480" MaxHeight="600" 
                    Style="{StaticResource CardBorder}" Padding="24" Background="White">
                <Border.Effect>
                    <DropShadowEffect Color="Black" Direction="270" ShadowDepth="4" BlurRadius="20" Opacity="0.2"/>
                </Border.Effect>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock x:Name="txtPanelTitle" Text="✨ Chi tiết Khuyến mãi" FontSize="18" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,20"/>
                    
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="0,0,0,20">
                        <StackPanel Margin="0,0,10,0">
                            <TextBlock Text="Mã khuyến mãi" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="0,0,0,4"/>
                            <TextBox x:Name="txtMaKhuyenMai" Style="{StaticResource ModernTextBox}" Margin="0,0,0,16" IsReadOnly="True" Background="#F8F9FA"/>

                            <TextBlock Text="Tên chương trình *" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="0,0,0,4"/>
                            <TextBox x:Name="txtTenChuongTrinh" Style="{StaticResource ModernTextBox}" Margin="0,0,0,16"/>

                            <Grid Margin="0,0,0,16">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="10"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock Text="Mức giảm *" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="0,0,0,4"/>
                                    <TextBox x:Name="txtMucGiam" Style="{StaticResource ModernTextBox}"/>
                                </StackPanel>
                                <StackPanel Grid.Column="2">
                                    <TextBlock Text="Loại giảm *" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="0,0,0,4"/>
                                    <ComboBox x:Name="cboLoaiGiam" Style="{StaticResource ModernComboBox}">
                                        <ComboBoxItem Content="%" IsSelected="True"/>
                                        <ComboBoxItem Content="VND"/>
                                    </ComboBox>
                                </StackPanel>
                            </Grid>

                            <TextBlock Text="Áp dụng theo" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="0,0,0,4"/>
                            <ComboBox x:Name="cboApDung" Style="{StaticResource ModernComboBox}" Margin="0,0,0,16">
                                <ComboBoxItem Content="Hóa đơn" IsSelected="True"/>
                                <ComboBoxItem Content="Danh mục"/>
                                <ComboBoxItem Content="Sản phẩm"/>
                            </ComboBox>

                            <Grid Margin="0,0,0,16">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="10"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock Text="Danh mục (nếu có)" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="0,0,0,4"/>
                                    <ComboBox x:Name="cboDanhMuc" Style="{StaticResource ModernComboBox}" SelectedValuePath="MaDanhMuc">
                                        <ComboBox.ItemTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding TenDanhMuc}"/>
                                            </DataTemplate>
                                        </ComboBox.ItemTemplate>
                                    </ComboBox>
                                </StackPanel>
                                <StackPanel Grid.Column="2">
                                    <TextBlock Text="Sản phẩm (nếu có)" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="0,0,0,4"/>
                                    <ComboBox x:Name="cboSanPham" Style="{StaticResource ModernComboBox}" SelectedValuePath="MaSanPham">
                                        <ComboBox.ItemTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding TenSanPham}"/>
                                            </DataTemplate>
                                        </ComboBox.ItemTemplate>
                                    </ComboBox>
                                </StackPanel>
                            </Grid>

                            <Grid Margin="0,0,0,16">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="10"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock Text="Ngày bắt đầu" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="0,0,0,4"/>
                                    <DatePicker x:Name="dpNgayBatDau" Style="{StaticResource ModernDatePicker}"/>
                                </StackPanel>
                                <StackPanel Grid.Column="2">
                                    <TextBlock Text="Ngày kết thúc" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="0,0,0,4"/>
                                    <DatePicker x:Name="dpNgayKetThuc" Style="{StaticResource ModernDatePicker}"/>
                                </StackPanel>
                            </Grid>

                            <TextBlock Text="Trạng thái" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="0,0,0,4"/>
                            <ComboBox x:Name="cboTrangThai" Style="{StaticResource ModernComboBox}">
                                <ComboBoxItem Content="Đang diễn ra" IsSelected="True"/>
                                <ComboBoxItem Content="Chưa bắt đầu"/>
                                <ComboBoxItem Content="Đã kết thúc"/>
                            </ComboBox>
                        </StackPanel>
                    </ScrollViewer>

                    <Grid Grid.Row="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Button Content="Hủy" Style="{StaticResource SecondaryButton}" Click="BtnCancel_Click"/>
                        <Button Grid.Column="2" Content="💾 Lưu lại" Style="{StaticResource PrimaryButton}" Click="BtnSave_Click"/>
                    </Grid>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
