<UserControl x:Class="LUnivers_Beaute.Views.DanhMucView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">
    <Grid>
        <Grid Margin="28,24" x:Name="mainContent">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="Danh mục Sản phẩm" Style="{StaticResource SectionTitle}"/>
                    <TextBlock Text="Phân loại các dòng sản phẩm mỹ phẩm" FontSize="13" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,4,0,0"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Button Content="📥 Xuất Excel" Style="{StaticResource SecondaryButton}" Margin="0,0,8,0" Padding="14,10"/>
                    <Button x:Name="btnAdd" Content="＋ Thêm danh mục" Style="{StaticResource PrimaryButton}" Padding="16,10" Click="BtnAdd_Click"/>
                </StackPanel>
            </Grid>
            <Border Grid.Row="1" Style="{StaticResource CardBorder}" Padding="16" Margin="0,0,0,16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="txtSearch" Style="{StaticResource ModernTextBox}" Margin="0,0,10,0"/>
                    <Button Grid.Column="1" Content="🔍 Tìm kiếm" Style="{StaticResource PrimaryButton}" Padding="16,10" Click="BtnSearch_Click"/>
                </Grid>
            </Border>
            <Border Grid.Row="2" Style="{StaticResource CardBorder}" Padding="20">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <ListBox x:Name="dgData" Grid.Row="0" BorderThickness="0" Background="Transparent" 
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                             ScrollViewer.VerticalScrollBarVisibility="Auto"
                             HorizontalContentAlignment="Stretch"
                             SelectionChanged="DgData_SelectionChanged">
                        <ListBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ListBox.ItemsPanel>
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="BorderThickness" Value="0"/>
                                <Setter Property="Padding" Value="0"/>
                                <Setter Property="Margin" Value="0,0,16,16"/>
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListBoxItem">
                                            <Border Background="{TemplateBinding Background}">
                                                <ContentPresenter/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="Transparent"/>
                                                </Trigger>
                                                <Trigger Property="IsSelected" Value="True">
                                                    <Setter Property="Background" Value="Transparent"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.ItemContainerStyle>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border Width="280" Height="130" Background="White" CornerRadius="12" Padding="16" BorderBrush="#F1F3F5" BorderThickness="1" Cursor="Hand">
                                    <Border.Effect>
                                        <DropShadowEffect Color="Black" Direction="270" ShadowDepth="2" BlurRadius="10" Opacity="0.05"/>
                                    </Border.Effect>
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="*"/>
                                        </Grid.RowDefinitions>
                                        
                                        <!-- Header -->
                                        <Grid Grid.Row="0">
                                            <Border Background="#FFF5F0" CornerRadius="8" Width="36" Height="36" HorizontalAlignment="Left">
                                                <TextBlock Text="📁" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                            </Border>
                                            <Border Background="#F8F9FA" CornerRadius="12" Padding="8,4" HorizontalAlignment="Right" VerticalAlignment="Top">
                                                <TextBlock Text="{Binding MaDanhMuc, StringFormat='Mã: {0}'}" FontSize="11" FontWeight="SemiBold" Foreground="{StaticResource TextSecondaryBrush}"/>
                                            </Border>
                                        </Grid>
                                        
                                        <!-- Bottom Content -->
                                        <Grid Grid.Row="1" Margin="0,16,0,0" VerticalAlignment="Bottom">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            
                                            <TextBlock Grid.Column="0" Text="{Binding TenDanhMuc}" FontSize="15" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" TextWrapping="Wrap" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                                            
                                            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="8,0,0,0">
                                                <Button Content="✏️" Style="{StaticResource IconButton}" Width="28" Height="28" Margin="0,0,4,0" Click="BtnEdit_Click" ToolTip="Cập nhật"/>
                                                <Button Content="🗑️" Style="{StaticResource IconButton}" Width="28" Height="28" Click="BtnDelete_Click" ToolTip="Xóa"/>
                                            </StackPanel>
                                        </Grid>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>

                    <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,16,0,0">
                        <Button x:Name="btnPrevPage" Content="&lt;" Style="{StaticResource SecondaryButton}" Width="36" Height="36" Padding="0" Click="BtnPrevPage_Click"/>
                        <TextBlock x:Name="txtPageInfo" Text="Trang 1 / 1" VerticalAlignment="Center" Margin="16,0" FontWeight="SemiBold"/>
                        <Button x:Name="btnNextPage" Content="&gt;" Style="{StaticResource SecondaryButton}" Width="36" Height="36" Padding="0" Click="BtnNextPage_Click"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>

        <!-- Overlay Modal Dialog -->
        <Grid x:Name="crudPanel" Visibility="Collapsed">
            <Border Background="#66000000" MouseDown="Overlay_MouseDown"/>
            
            <Border HorizontalAlignment="Center" VerticalAlignment="Center" Width="400" MaxHeight="600" 
                    Style="{StaticResource CardBorder}" Padding="24" Background="White">
                <Border.Effect>
                    <DropShadowEffect Color="Black" Direction="270" ShadowDepth="4" BlurRadius="20" Opacity="0.2"/>
                </Border.Effect>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock x:Name="txtPanelTitle" Text="✨ Chi tiết Danh mục" FontSize="18" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,20"/>
                    
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="0,0,0,20">
                        <StackPanel Margin="0,0,10,0">
                            <TextBlock Text="Mã danh mục" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="0,0,0,4"/>
                            <TextBox x:Name="txtMaDanhMuc" Style="{StaticResource ModernTextBox}" Margin="0,0,0,16" IsReadOnly="True" Background="#F8F9FA"/>

                            <TextBlock Text="Tên danh mục *" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="0,0,0,4"/>
                            <TextBox x:Name="txtTenDanhMuc" Style="{StaticResource ModernTextBox}" Margin="0,0,0,16"/>
                        </StackPanel>
                    </ScrollViewer>

                    <Grid Grid.Row="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Button Content="Hủy" Style="{StaticResource SecondaryButton}" Click="BtnCancel_Click"/>
                        <Button Grid.Column="2" Content="💾 Lưu lại" Style="{StaticResource PrimaryButton}" Click="BtnSave_Click"/>
                    </Grid>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>

