<UserControl x:Class="LUnivers_Beaute.Views.KhachHangView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">
    <Grid>
        <Grid Margin="28,24" x:Name="mainContent">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="Quản lý Khách hàng" Style="{StaticResource SectionTitle}"/>
                    <TextBlock Text="Danh sách khách hàng thân thiết và điểm tích lũy" FontSize="13" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,4,0,0"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Button Content="📥 Xuất Excel" Style="{StaticResource SecondaryButton}" Margin="0,0,8,0" Padding="14,10"/>
                    <Button x:Name="btnAdd" Content="＋ Thêm khách hàng" Style="{StaticResource PrimaryButton}" Padding="16,10" Click="BtnAdd_Click"/>
                </StackPanel>
            </Grid>
            <Border Grid.Row="1" Style="{StaticResource CardBorder}" Padding="16" Margin="0,0,0,16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="160"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox Style="{StaticResource ModernTextBox}" Margin="0,0,10,0"/>
                    <ComboBox Grid.Column="1" Style="{StaticResource ModernComboBox}" Margin="0,0,10,0">
                        <ComboBoxItem Content="Trạng thái" IsSelected="True"/>
                    </ComboBox>
                    <Button Grid.Column="2" Content="🔍 Tìm kiếm" Style="{StaticResource PrimaryButton}" Padding="16,10"/>
                </Grid>
            </Border>
            <Border Grid.Row="2" Style="{StaticResource CardBorder}" Padding="0">
                <Grid><Grid.RowDefinitions><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions><DataGrid AutoGenerateColumns="False" x:Name="dgData" Style="{StaticResource ModernDataGrid}"
                          ColumnHeaderStyle="{StaticResource ModernDataGridColumnHeader}"
                          RowStyle="{StaticResource ModernDataGridRow}"
                          CellStyle="{StaticResource ModernDataGridCell}"
                          SelectionChanged="DgData_SelectionChanged">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="MÃ KH" Binding="{Binding MaKhachHang}" Width="80"/>
                        <DataGridTextColumn Header="HỌ TÊN" Binding="{Binding HoTen}" Width="*"/>
                        <DataGridTextColumn Header="SỐ ĐIỆN THOẠI" Binding="{Binding SoDienThoai}" Width="140"/>
                        <DataGridTextColumn Header="ĐIỂM TÍCH LŨY" Binding="{Binding DiemTichLuy}" Width="130"/>
                        <DataGridTextColumn Header="TRẠNG THÁI" Binding="{Binding TrangThai}" Width="110"/>
                        <DataGridTemplateColumn Header="THAO TÁC" Width="120">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Content="✏️" Style="{StaticResource IconButton}" Width="30" Height="30" Click="BtnEdit_Click"/>
                                        <Button Content="🗑️" Style="{StaticResource IconButton}" Width="30" Height="30"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,12,0,12">
            <Button x:Name="btnPrevPage" Content="&lt;" Style="{StaticResource SecondaryButton}" Width="36" Height="36" Padding="0" Click="BtnPrevPage_Click"/>
            <TextBlock x:Name="txtPageInfo" Text="Trang 1 / 1" VerticalAlignment="Center" Margin="16,0" FontWeight="SemiBold"/>
            <Button x:Name="btnNextPage" Content="&gt;" Style="{StaticResource SecondaryButton}" Width="36" Height="36" Padding="0" Click="BtnNextPage_Click"/>
        </StackPanel>
</Grid>
            </Border>
        </Grid>

        <!-- Overlay Modal Dialog -->
        <Grid x:Name="crudPanel" Visibility="Collapsed">
            <!-- Dark Translucent Backdrop -->
            <Border Background="#66000000" MouseDown="Overlay_MouseDown"/>
            
            <!-- Center Card -->
            <Border HorizontalAlignment="Center" VerticalAlignment="Center" Width="450" MaxHeight="600" 
                    Style="{StaticResource CardBorder}" Padding="24" Background="White">
                <Border.Effect>
                    <DropShadowEffect Color="Black" Direction="270" ShadowDepth="4" BlurRadius="20" Opacity="0.2"/>
                </Border.Effect>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock x:Name="txtPanelTitle" Text="✨ Chi tiết Khách hàng" FontSize="18" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,20"/>
                    
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="0,0,0,20">
                        <StackPanel Margin="0,0,10,0">
                            <TextBlock Text="Mã khách hàng" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="0,0,0,4"/>
                            <TextBox x:Name="txtMaKhachHang" Style="{StaticResource ModernTextBox}" Margin="0,0,0,16" IsReadOnly="True" Background="#F8F9FA"/>

                            <TextBlock Text="Họ tên *" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="0,0,0,4"/>
                            <TextBox x:Name="txtHoTen" Style="{StaticResource ModernTextBox}" Margin="0,0,0,16"/>

                            <TextBlock Text="Số điện thoại *" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="0,0,0,4"/>
                            <TextBox x:Name="txtSoDienThoai" Style="{StaticResource ModernTextBox}" Margin="0,0,0,16"/>

                            <TextBlock Text="Điểm tích lũy" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="0,0,0,4"/>
                            <TextBox x:Name="txtDiemTichLuy" Style="{StaticResource ModernTextBox}" Margin="0,0,0,16"/>

                            <TextBlock Text="Trạng thái" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="0,0,0,4"/>
                            <ComboBox x:Name="cboTrangThai" Style="{StaticResource ModernComboBox}" Margin="0,0,0,16" IsEditable="True">
                                <ComboBoxItem Content="Hoạt động" IsSelected="True"/>
                                <ComboBoxItem Content="Khóa"/>
                            </ComboBox>
                        </StackPanel>
                    </ScrollViewer>

                    <Grid Grid.Row="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Button Content="Hủy" Style="{StaticResource SecondaryButton}" Click="BtnCancel_Click"/>
                        <Button Grid.Column="2" Content="💾 Lưu lại" Style="{StaticResource PrimaryButton}"/>
                    </Grid>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>

