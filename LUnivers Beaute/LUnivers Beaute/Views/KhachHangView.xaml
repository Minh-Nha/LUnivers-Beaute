<UserControl x:Class="LUnivers_Beaute.Views.KhachHangView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">
    <Grid>
        <Grid Margin="28,24" x:Name="mainContent">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <!-- Page Header -->
            <Grid Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="Quản lý Khách hàng" Style="{StaticResource SectionTitle}"/>
                    <TextBlock Text="Danh sách khách hàng thân thiết và điểm tích lũy" FontSize="13" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,4,0,0"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Button Content="📥 Xuất Excel" Style="{StaticResource SecondaryButton}" Margin="0,0,8,0" Padding="14,10"/>
                    <Button x:Name="btnAdd" Content="＋ Thêm khách hàng" Style="{StaticResource PrimaryButton}" Padding="16,10" Click="BtnAdd_Click"/>
                </StackPanel>
            </Grid>
            
            <!-- Stat Cards -->
            <UniformGrid Grid.Row="1" Rows="1" Columns="3" Margin="0,0,0,16">
                <Border Style="{StaticResource StatCard}" Margin="0,0,8,0">
                    <StackPanel>
                        <Border Width="36" Height="36" CornerRadius="18" Background="#E8F5E9" Margin="0,0,0,10" HorizontalAlignment="Left">
                            <TextBlock Text="👥" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <TextBlock Text="Tổng khách hàng" FontSize="11" Foreground="#7A7A7A" Margin="0,0,0,4"/>
                        <TextBlock x:Name="txtTongKH" Text="0" FontSize="20" FontWeight="Bold" Foreground="#2D2D2D"/>
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource StatCard}" Margin="4,0,4,0">
                    <StackPanel>
                        <Border Width="36" Height="36" CornerRadius="18" Background="#E3F2FD" Margin="0,0,0,10" HorizontalAlignment="Left">
                            <TextBlock Text="✅" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <TextBlock Text="Đang hoạt động" FontSize="11" Foreground="#7A7A7A" Margin="0,0,0,4"/>
                        <TextBlock x:Name="txtHoatDong" Text="0" FontSize="20" FontWeight="Bold" Foreground="#2E7D32"/>
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource StatCard}" Margin="8,0,0,0">
                    <StackPanel>
                        <Border Width="36" Height="36" CornerRadius="18" Background="#FFF3E0" Margin="0,0,0,10" HorizontalAlignment="Left">
                            <TextBlock Text="⭐" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <TextBlock Text="Tổng điểm tích lũy" FontSize="11" Foreground="#7A7A7A" Margin="0,0,0,4"/>
                        <TextBlock x:Name="txtTongDiem" Text="0" FontSize="20" FontWeight="Bold" Foreground="#F5A623"/>
                    </StackPanel>
                </Border>
            </UniformGrid>
            
            <!-- Search & Filter Bar -->
            <Border Grid.Row="2" Style="{StaticResource CardBorder}" Padding="16" Margin="0,0,0,16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="180"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <Border Grid.Column="0" Background="White" CornerRadius="6" Padding="12,0" Margin="0,0,10,0" BorderBrush="#E0E0E0" BorderThickness="1">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="🔍" VerticalAlignment="Center" Foreground="#7A7A7A" Margin="0,0,8,0"/>
                            <TextBox x:Name="txtSearch" Grid.Column="1" Background="Transparent" BorderThickness="0" VerticalAlignment="Center" Margin="0,6"/>
                            <TextBlock Text="Tìm theo tên, số điện thoại..." Foreground="#B0B0B0" VerticalAlignment="Center" Grid.Column="1" IsHitTestVisible="False">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Text, ElementName=txtSearch}" Value="">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Text, ElementName=txtSearch}" Value="{x:Null}">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Grid>
                    </Border>
                    
                    <ComboBox x:Name="cmbTrangThai" Grid.Column="1" Style="{StaticResource ModernComboBox}" Margin="0,0,10,0">
                        <ComboBoxItem Content="Tất cả trạng thái" IsSelected="True"/>
                        <ComboBoxItem Content="Hoạt động"/>
                        <ComboBoxItem Content="Khóa"/>
                    </ComboBox>
                    
                    <Button Grid.Column="2" Content="🔍 Tìm kiếm" Style="{StaticResource PrimaryButton}" Padding="24,10" Click="BtnSearch_Click"/>
                </Grid>
            </Border>
            
            <!-- Data Grid -->
            <Border Grid.Row="3" Style="{StaticResource CardBorder}" Padding="0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <DataGrid AutoGenerateColumns="False" x:Name="dgData" Style="{StaticResource ModernDataGrid}"
                              ColumnHeaderStyle="{StaticResource ModernDataGridColumnHeader}"
                              RowStyle="{StaticResource ModernDataGridRow}"
                              CellStyle="{StaticResource ModernDataGridCell}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="MÃ KH" Binding="{Binding MaKhachHang}" Width="80">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="FontWeight" Value="SemiBold"/>
                                        <Setter Property="Foreground" Value="{StaticResource AccentGreenBrush}"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="HỌ TÊN" Binding="{Binding HoTen}" Width="*">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="FontWeight" Value="SemiBold"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="SỐ ĐIỆN THOẠI" Binding="{Binding SoDienThoai}" Width="150"/>
                            <DataGridTemplateColumn Header="ĐIỂM TÍCH LŨY" Width="130">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border CornerRadius="4" Padding="8,4" Background="#FFF8E1" HorizontalAlignment="Left">
                                            <TextBlock FontSize="12" FontWeight="Bold" Foreground="#F5A623">
                                                <Run Text="⭐ "/><Run Text="{Binding DiemTichLuy, Mode=OneWay}"/>
                                            </TextBlock>
                                        </Border>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Header="TRẠNG THÁI" Width="120">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border CornerRadius="4" Padding="8,4" HorizontalAlignment="Left" VerticalAlignment="Center">
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Setter Property="Background" Value="#E8F5E9"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding TrangThai}" Value="Khóa">
                                                            <Setter Property="Background" Value="#FFEBEE"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                            <TextBlock Text="{Binding TrangThai}" FontSize="12" FontWeight="SemiBold">
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Foreground" Value="#2E7D32"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding TrangThai}" Value="Khóa">
                                                                <Setter Property="Foreground" Value="#C62828"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </Border>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Header="THAO TÁC" Width="100">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Button Content="✏️" Style="{StaticResource IconButton}" Width="30" Height="30" Click="BtnEdit_Click" ToolTip="Sửa"/>
                                            <Button Content="🗑️" Style="{StaticResource IconButton}" Width="30" Height="30" Click="BtnDelete_Click" ToolTip="Xóa"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                    <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,12,0,12">
                        <Button x:Name="btnPrevPage" Content="&lt;" Style="{StaticResource SecondaryButton}" Width="36" Height="36" Padding="0" Click="BtnPrevPage_Click"/>
                        <TextBlock x:Name="txtPageInfo" Text="Trang 1 / 1" VerticalAlignment="Center" Margin="16,0" FontWeight="SemiBold"/>
                        <Button x:Name="btnNextPage" Content="&gt;" Style="{StaticResource SecondaryButton}" Width="36" Height="36" Padding="0" Click="BtnNextPage_Click"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>

        <!-- Overlay Modal Dialog -->
        <Grid x:Name="crudPanel" Visibility="Collapsed">
            <Border Background="#66000000" MouseDown="Overlay_MouseDown"/>
            
            <Border HorizontalAlignment="Center" VerticalAlignment="Center" Width="460" MaxHeight="600" 
                    Style="{StaticResource CardBorder}" Padding="28" Background="White">
                <Border.Effect>
                    <DropShadowEffect Color="Black" Direction="270" ShadowDepth="4" BlurRadius="20" Opacity="0.2"/>
                </Border.Effect>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <!-- Modal Header -->
                    <Grid Margin="0,0,0,20">
                        <TextBlock x:Name="txtPanelTitle" Text="✨ Thêm Khách hàng" FontSize="18" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
                        <Button Content="✕" HorizontalAlignment="Right" Background="Transparent" BorderThickness="0" FontSize="16" Foreground="{StaticResource TextSecondaryBrush}" Click="BtnCancel_Click"/>
                    </Grid>
                    
                    <!-- Form Fields -->
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="0,0,0,20">
                        <StackPanel Margin="0,0,6,0">
                            <TextBlock Text="Mã khách hàng" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="0,0,0,6"/>
                            <TextBox x:Name="txtMaKhachHang" Style="{StaticResource ModernTextBox}" Margin="0,0,0,18" IsReadOnly="True" Background="#F8F9FA"/>

                            <TextBlock Text="Họ tên *" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="0,0,0,6"/>
                            <TextBox x:Name="txtHoTen" Style="{StaticResource ModernTextBox}" Margin="0,0,0,18"/>

                            <TextBlock Text="Số điện thoại *" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="0,0,0,6"/>
                            <TextBox x:Name="txtSoDienThoai" Style="{StaticResource ModernTextBox}" Margin="0,0,0,18"/>

                            <TextBlock Text="Điểm tích lũy" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="0,0,0,6"/>
                            <TextBox x:Name="txtDiemTichLuy" Style="{StaticResource ModernTextBox}" Margin="0,0,0,18"/>

                            <TextBlock Text="Trạng thái" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="0,0,0,6"/>
                            <ComboBox x:Name="cboTrangThai" Style="{StaticResource ModernComboBox}">
                                <ComboBoxItem Content="Hoạt động" IsSelected="True"/>
                                <ComboBoxItem Content="Khóa"/>
                            </ComboBox>
                        </StackPanel>
                    </ScrollViewer>

                    <!-- Action Buttons -->
                    <Grid Grid.Row="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="12"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Button Content="Hủy" Style="{StaticResource SecondaryButton}" Click="BtnCancel_Click" Height="42"/>
                        <Button Grid.Column="2" Content="💾 Lưu lại" Style="{StaticResource PrimaryButton}" Click="BtnSave_Click" Height="42"/>
                    </Grid>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
