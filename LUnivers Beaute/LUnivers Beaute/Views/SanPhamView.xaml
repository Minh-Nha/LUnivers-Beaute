<UserControl x:Class="LUnivers_Beaute.Views.SanPhamView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">
    <Grid>
        <!-- Main Content -->
        <Grid Margin="28,24" x:Name="mainContent">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Grid.Row="0" Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="Quản lý Sản phẩm" Style="{StaticResource SectionTitle}"/>
                    <TextBlock Text="Quản lý danh sách sản phẩm mỹ phẩm" FontSize="13" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,4,0,0"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Button Content="📥 Xuất Excel" Style="{StaticResource SecondaryButton}" Margin="0,0,8,0" Padding="14,10"/>
                    <Button x:Name="btnAdd" Content="＋ Thêm sản phẩm" Style="{StaticResource PrimaryButton}" Padding="16,10" Click="BtnAdd_Click"/>
                </StackPanel>
            </Grid>

            <!-- Filters -->
            <Border Grid.Row="1" Style="{StaticResource CardBorder}" Padding="16" Margin="0,0,0,16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="160"/>
                        <ColumnDefinition Width="160"/>
                        <ColumnDefinition Width="160"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="txtSearch" Style="{StaticResource ModernTextBox}" Margin="0,0,10,0"/>
                    <ComboBox x:Name="cboFilterDanhMuc" Grid.Column="1" Style="{StaticResource ModernComboBox}" Margin="0,0,10,0" SelectedValuePath="MaDanhMuc" IsEditable="False">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding TenDanhMuc}"/>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                    <ComboBox x:Name="cboFilterThuongHieu" Grid.Column="2" Style="{StaticResource ModernComboBox}" Margin="0,0,10,0" SelectedValuePath="MaThuongHieu" IsEditable="False">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding TenThuongHieu}"/>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                    <ComboBox x:Name="cboFilterTrangThai" Grid.Column="3" Style="{StaticResource ModernComboBox}" Margin="0,0,10,0" IsEditable="False">
                        <ComboBoxItem Content="Tất cả trạng thái" Tag="-1" IsSelected="True"/>
                        <ComboBoxItem Content="Đang bán" Tag="1"/>
                        <ComboBoxItem Content="Ngừng kinh doanh" Tag="0"/>
                    </ComboBox>
                    <Button Grid.Column="4" Content="🔍 Tìm kiếm" Style="{StaticResource PrimaryButton}" Padding="16,10" Click="BtnSearch_Click"/>
                </Grid>
            </Border>

            <!-- Data Table -->
            <Border Grid.Row="2" Style="{StaticResource CardBorder}" Padding="0">
                <Grid><Grid.RowDefinitions><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions><DataGrid AutoGenerateColumns="False" x:Name="dgData" Style="{StaticResource ModernDataGrid}"
                          ColumnHeaderStyle="{StaticResource ModernDataGridColumnHeader}"
                          RowStyle="{StaticResource ModernDataGridRow}"
                          CellStyle="{StaticResource ModernDataGridCell}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="MÃ SP" Binding="{Binding MaSanPham}" Width="100"/>
                        <DataGridTextColumn Header="TÊN SẢN PHẨM" Binding="{Binding TenSanPham}" Width="*"/>
                        <DataGridTextColumn Header="DANH MỤC" Binding="{Binding TenDanhMuc}" Width="120"/>
                        <DataGridTextColumn Header="THƯƠNG HIỆU" Binding="{Binding TenThuongHieu}" Width="120"/>
                        <DataGridTextColumn Header="ĐƠN VỊ" Binding="{Binding DonViTinh}" Width="70"/>
                        <DataGridTextColumn Header="GIÁ NIÊM YẾT" Binding="{Binding GiaNiemYet}" Width="120"/>
                        <DataGridTextColumn Header="TRẠNG THÁI" Binding="{Binding TrangThaiStr}" Width="100"/>
                        <DataGridTemplateColumn Header="THAO TÁC" Width="120">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Button Content="✏️" Style="{StaticResource IconButton}" Width="30" Height="30" Click="BtnEdit_Click"/>
                                        <Button Content="🗑️" Style="{StaticResource IconButton}" Width="30" Height="30" Click="BtnDelete_Click"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,12,0,12">
            <Button x:Name="btnPrevPage" Content="&lt;" Style="{StaticResource SecondaryButton}" Width="36" Height="36" Padding="0" Click="BtnPrevPage_Click"/>
            <TextBlock x:Name="txtPageInfo" Text="Trang 1 / 1" VerticalAlignment="Center" Margin="16,0" FontWeight="SemiBold"/>
            <Button x:Name="btnNextPage" Content="&gt;" Style="{StaticResource SecondaryButton}" Width="36" Height="36" Padding="0" Click="BtnNextPage_Click"/>
        </StackPanel>
</Grid>
            </Border>
        </Grid>

        <!-- Overlay Modal Dialog -->
        <Grid x:Name="crudPanel" Visibility="Collapsed">
            <!-- Dark Translucent Backdrop -->
            <Border Background="#66000000" MouseDown="Overlay_MouseDown"/>
            
            <!-- Center Card -->
            <Border HorizontalAlignment="Center" VerticalAlignment="Center" Width="650" MaxHeight="600" 
                    Style="{StaticResource CardBorder}" Padding="24" Background="White">
                <Border.Effect>
                    <DropShadowEffect Color="Black" Direction="270" ShadowDepth="4" BlurRadius="20" Opacity="0.2"/>
                </Border.Effect>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock x:Name="txtPanelTitle" Text="✨ Chi tiết Sản phẩm" FontSize="18" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,20"/>
                    
                    <Grid Grid.Row="1" Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="180"/>
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Product Image Area -->
                        <StackPanel Grid.Column="0">
                            <Border Width="160" Height="160" CornerRadius="8" Background="#F1F3F5" BorderBrush="#E9ECEF" BorderThickness="1" Margin="0,0,0,12">
                                <Grid>
                                    <TextBlock Text="🖼️" FontSize="40" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#ADB5BD"/>
                                    <Image x:Name="imgSanPham" Stretch="UniformToFill">
                                        <Image.Clip>
                                            <RectangleGeometry Rect="0,0,160,160" RadiusX="8" RadiusY="8"/>
                                        </Image.Clip>
                                    </Image>
                                </Grid>
                            </Border>
                            <Button Content="Tải ảnh lên" Style="{StaticResource SecondaryButton}"/>
                        </StackPanel>

                        <!-- Product Details Form -->
                        <ScrollViewer Grid.Column="2" VerticalScrollBarVisibility="Auto">
                            <StackPanel Margin="0,0,10,0">
                                <TextBlock Text="Mã sản phẩm *" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="0,0,0,4"/>
                                <TextBox x:Name="txtMaSanPham" Style="{StaticResource ModernTextBox}" Margin="0,0,0,16" Background="#F8F9FA"/>

                                <TextBlock Text="Tên sản phẩm *" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="0,0,0,4"/>
                                <TextBox x:Name="txtTenSanPham" Style="{StaticResource ModernTextBox}" Margin="0,0,0,16"/>

                                <Grid Margin="0,0,0,16">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="10"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="Danh mục *" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="0,0,0,4"/>
                                        <ComboBox x:Name="cboDanhMuc" Style="{StaticResource ModernComboBox}" SelectedValuePath="MaDanhMuc" IsEditable="False">
                                            <ComboBox.ItemTemplate>
                                                <DataTemplate>
                                                    <TextBlock Text="{Binding TenDanhMuc}"/>
                                                </DataTemplate>
                                            </ComboBox.ItemTemplate>
                                        </ComboBox>
                                    </StackPanel>
                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="Thương hiệu *" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="0,0,0,4"/>
                                        <ComboBox x:Name="cboThuongHieu" Style="{StaticResource ModernComboBox}" SelectedValuePath="MaThuongHieu" IsEditable="False">
                                            <ComboBox.ItemTemplate>
                                                <DataTemplate>
                                                    <TextBlock Text="{Binding TenThuongHieu}"/>
                                                </DataTemplate>
                                            </ComboBox.ItemTemplate>
                                        </ComboBox>
                                    </StackPanel>
                                </Grid>
                                
                                <TextBlock Text="Nhà cung cấp *" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="0,0,0,4"/>
                                <ComboBox x:Name="cboNhaCungCap" Style="{StaticResource ModernComboBox}" Margin="0,0,0,16" SelectedValuePath="MaNhaCungCap" IsEditable="False">
                                    <ComboBox.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding TenNhaCungCap}"/>
                                        </DataTemplate>
                                    </ComboBox.ItemTemplate>
                                </ComboBox>

                                <Grid Margin="0,0,0,16">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="10"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="Đơn vị tính" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="0,0,0,4"/>
                                        <TextBox x:Name="txtDonViTinh" Style="{StaticResource ModernTextBox}"/>
                                    </StackPanel>
                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="Giá niêm yết *" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="0,0,0,4"/>
                                        <TextBox x:Name="txtGiaNiemYet" Style="{StaticResource ModernTextBox}"/>
                                    </StackPanel>
                                </Grid>

                                <TextBlock Text="Trạng thái" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="0,0,0,4"/>
                                <ComboBox x:Name="cboTrangThai" Style="{StaticResource ModernComboBox}" Margin="0,0,0,16">
                                    <ComboBoxItem Content="Đang bán" IsSelected="True"/>
                                    <ComboBoxItem Content="Ngừng kinh doanh"/>
                                </ComboBox>

                                <TextBlock Text="Mô tả" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="0,0,0,4"/>
                                <TextBox x:Name="txtMoTa" Style="{StaticResource ModernTextBox}" Height="80" TextWrapping="Wrap" AcceptsReturn="True"/>
                            </StackPanel>
                        </ScrollViewer>
                    </Grid>

                    <Grid Grid.Row="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Button Content="Hủy" Style="{StaticResource SecondaryButton}" Click="BtnCancel_Click"/>
                        <Button Grid.Column="2" Content="💾 Lưu lại" Style="{StaticResource PrimaryButton}" Click="BtnSave_Click"/>
                    </Grid>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>

