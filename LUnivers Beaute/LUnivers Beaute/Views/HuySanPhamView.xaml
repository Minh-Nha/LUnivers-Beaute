<UserControl x:Class="LUnivers_Beaute.Views.HuySanPhamView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">
    <Grid>
        <Grid Margin="28,24" x:Name="mainContent">
            <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Margin="0,0,0,20">
            <StackPanel>
                <TextBlock Text="Hủy sản phẩm" Style="{StaticResource SectionTitle}"/>
                <TextBlock Text="Quản lý sản phẩm quá hạn sử dụng cần hủy" FontSize="13" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,4,0,0"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                <Button x:Name="btnChoHuy" Content="⚠️ Chờ hủy" Style="{StaticResource SecondaryButton}" Margin="0,0,8,0" Click="BtnChoHuy_Click"/>
                <Button x:Name="btnAdd" Content="＋ Tạo phiếu hủy" Style="{StaticResource DangerButton}" Click="BtnAdd_Click"/>
            </StackPanel>
        </Grid>
        <Border Grid.Row="1" Style="{StaticResource CardBorder}" Padding="0">
            <Grid><Grid.RowDefinitions><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions><DataGrid AutoGenerateColumns="False" x:Name="dgData" Style="{StaticResource ModernDataGrid}" ColumnHeaderStyle="{StaticResource ModernDataGridColumnHeader}"
                      RowStyle="{StaticResource ModernDataGridRow}" CellStyle="{StaticResource ModernDataGridCell}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="MÃ HỦY" Binding="{Binding MaHuy}" Width="80"/>
                    <DataGridTextColumn Header="CỬA HÀNG" Binding="{Binding TenCuaHang}" Width="140"/>
                    <DataGridTextColumn Header="NHÂN VIÊN" Binding="{Binding NhanVienHuy}" Width="140"/>
                    <DataGridTextColumn Header="SẢN PHẨM / LÔ" Binding="{Binding TenSanPham}" Width="*"/>
                    <DataGridTextColumn Header="SỐ LƯỢNG" Binding="{Binding SoLuong}" Width="100"/>
                    <DataGridTextColumn Header="NGÀY HỦY" Binding="{Binding NgayHuy}" Width="140"/>
                    <DataGridTextColumn Header="LÝ DO" Binding="{Binding LyDo}" Width="200"/>
                </DataGrid.Columns>
            </DataGrid>
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,12,0,12">
            <Button x:Name="btnPrevPage" Content="&lt;" Style="{StaticResource SecondaryButton}" Width="36" Height="36" Padding="0" Click="BtnPrevPage_Click"/>
            <TextBlock x:Name="txtPageInfo" Text="Trang 1 / 1" VerticalAlignment="Center" Margin="16,0" FontWeight="SemiBold"/>
            <Button x:Name="btnNextPage" Content="&gt;" Style="{StaticResource SecondaryButton}" Width="36" Height="36" Padding="0" Click="BtnNextPage_Click"/>
        </StackPanel>
</Grid>
        </Border>
        </Grid>

        <!-- Overlay Modal Dialog -->
        <Grid x:Name="crudPanel" Visibility="Collapsed">
            <Border Background="#66000000" MouseDown="Overlay_MouseDown"/>
            
            <Border HorizontalAlignment="Center" VerticalAlignment="Center" Width="450" MaxHeight="600" 
                    Style="{StaticResource CardBorder}" Padding="24" Background="White">
                <Border.Effect>
                    <DropShadowEffect Color="Black" Direction="270" ShadowDepth="4" BlurRadius="20" Opacity="0.2"/>
                </Border.Effect>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock x:Name="txtPanelTitle" Text="✨ Phiếu hủy sản phẩm" FontSize="18" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,20"/>
                    
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="0,0,0,20">
                        <StackPanel Margin="0,0,10,0">
                            <TextBlock Text="Cửa hàng *" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="0,0,0,4"/>
                            <ComboBox x:Name="cboCuaHang" Style="{StaticResource ModernComboBox}" Margin="0,0,0,16"/>

                            <TextBlock Text="Nhân viên hủy *" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="0,0,0,4"/>
                            <ComboBox x:Name="cboNhanVien" Style="{StaticResource ModernComboBox}" Margin="0,0,0,16"/>

                            <TextBlock Text="Sản phẩm / Lô *" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="0,0,0,4"/>
                            <ComboBox x:Name="cboLoSanPham" Style="{StaticResource ModernComboBox}" Margin="0,0,0,16"/>

                            <Grid Margin="0,0,0,16">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="10"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock Text="Số lượng *" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="0,0,0,4"/>
                                    <TextBox x:Name="txtSoLuong" Style="{StaticResource ModernTextBox}"/>
                                </StackPanel>
                                <StackPanel Grid.Column="2">
                                    <TextBlock Text="Ngày hủy *" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="0,0,0,4"/>
                                    <DatePicker x:Name="dpNgayHuy" Style="{StaticResource ModernDatePicker}"/>
                                </StackPanel>
                            </Grid>

                            <TextBlock Text="Lý do *" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="0,0,0,4"/>
                            <TextBox x:Name="txtLyDo" Style="{StaticResource ModernTextBox}" Height="60" TextWrapping="Wrap" AcceptsReturn="True" Margin="0,0,0,16" Text="Sản phẩm quá hạn sử dụng"/>
                        </StackPanel>
                    </ScrollViewer>

                    <Grid Grid.Row="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Button Content="Hủy" Style="{StaticResource SecondaryButton}" Click="BtnCancel_Click"/>
                        <Button Grid.Column="2" Content="💾 Lưu lại" Style="{StaticResource DangerButton}" Click="BtnSave_Click"/>
                    </Grid>
                </Grid>
            </Border>
        </Grid>
        <!-- Cho Huy Panel (Sản phẩm hết hạn cần duyệt) -->
        <Grid x:Name="choHuyPanel" Visibility="Collapsed">
            <Border Background="#66000000" MouseDown="OverlayChoHuy_MouseDown"/>
            
            <Border HorizontalAlignment="Center" VerticalAlignment="Center" Width="700" MaxHeight="600" 
                    Style="{StaticResource CardBorder}" Padding="24" Background="White">
                <Border.Effect>
                    <DropShadowEffect Color="Black" Direction="270" ShadowDepth="4" BlurRadius="20" Opacity="0.2"/>
                </Border.Effect>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Text="⚠️ Danh sách sản phẩm chờ hủy" FontSize="18" FontWeight="Bold" Foreground="#D32F2F" Margin="0,0,0,16"/>
                    
                    <DataGrid AutoGenerateColumns="False" x:Name="dgChoHuy" Grid.Row="1" Style="{StaticResource ModernDataGrid}" ColumnHeaderStyle="{StaticResource ModernDataGridColumnHeader}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="SẢN PHẨM" Binding="{Binding TenSanPham}" Width="*"/>
                            <DataGridTextColumn Header="LÔ" Binding="{Binding SoLo}" Width="80"/>
                            <DataGridTextColumn Header="TỒN" Binding="{Binding SoLuongTon}" Width="60"/>
                            <DataGridTextColumn Header="HSD" Binding="{Binding HanSuDung, StringFormat=dd/MM/yyyy}" Width="100"/>
                            <DataGridTemplateColumn Header="THAO TÁC" Width="90">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="Duyệt" Style="{StaticResource DangerButton}" Height="30" Padding="12,0" Click="BtnDuyetHuy_Click"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>









